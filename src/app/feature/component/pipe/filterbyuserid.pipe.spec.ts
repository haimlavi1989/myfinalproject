import { FilterbyuseridPipe } from './filterbyuserid.pipe';

describe('FilterbyuseridPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyuseridPipe();
    expect(pipe).toBeTruthy();
  });
});
